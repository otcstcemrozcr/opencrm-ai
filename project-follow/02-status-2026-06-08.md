# Status Snapshot — 2026-06-08

## Phase
MVP-0 build in progress. Sprints 0–2 complete and pushed.

## Shipped (on GitHub: otcstcemrozcr/opencrm-ai)
| Sprint | Scope | Commit |
|---|---|---|
| 0 | App skeleton, own JWT+RBAC auth, Drizzle/Neon, app shell | `211457a` |
| 1 | Accounts, Contacts, Leads, transactional Convert | `56b1dcd` |
| 2 | Opportunities CRUD, Kanban pipeline (drag stage), minimal Activities | `3365d7f` |
| — | Login split-screen panel; temp-script cleanup | `daac112`, `588424f` |

## What works (verified)
- Auth: sign-up (creates org + admin), login, logout, route gating (middleware).
- CRM: Accounts / Contacts / Leads CRUD; Lead → Account+Contact+Opportunity convert (transactional).
- Pipeline: Opportunities CRUD; Kanban drag between 8 stages (optimistic + persisted); List/Kanban toggle.
- Activities: add/list on opportunity detail; bumps `last_activity_at`.
- Build green (25 routes, TS strict); DB-level tests pass; cross-tenant isolation = 0 leak.

## Stack / infra
Next.js 14 + TS + Tailwind + shadcn-style UI · Postgres (Neon) + Drizzle · own JWT (`jose`) +
argon2 (`@node-rs/argon2`) · React-PDF (planned Sprint 3) · Anthropic (planned). DB live on Neon.
Local dev: `npm run dev` (≈ localhost:3000).

## Known issues
- Local dev server is flaky on this Windows env (exits 255 across turns). Not a code issue —
  build + logic tests pass. Restart with `npm run dev`.

## Next
1. Sprint 3 — Dashboard KPIs (deterministic; no external key).
2. Sprint 3 — Quotes + PDF export (React-PDF).
3. AI Insight v1 (needs `ANTHROPIC_API_KEY`).
4. Sprint 1 leftover — Resend invitations + password reset (needs `RESEND_API_KEY`).

## Decisions still locked
Own JWT+RBAC · Anthropic AI (agnostic) · React-PDF · app-layer tenant isolation · local-first ·
stages New→Qualified→Discovery→Meeting→Proposal→Negotiation→Won/Lost · roles Admin/Manager/Rep/Viewer.
